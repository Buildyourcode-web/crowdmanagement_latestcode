# Core
insightface>=0.7.3
onnxruntime-gpu>=1.16.0   # use onnxruntime (CPU-only) if no CUDA
opencv-python>=4.8.0
numpy>=1.24.0
python-dotenv>=1.0.0

# Optional — needed only for RTSP with H.264/H.265
# opencv-contrib-python can replace opencv-python if codec issues arise

# Future experiments (install when needed, not now):
# deep-sort-realtime   # R0 optional tracker
# torch                # R3 FSPFM / R4 FaceLift
# torchvision          # R3+
